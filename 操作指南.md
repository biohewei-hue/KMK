# 操作指南（给非技术用户）

全程只需要**双击**，不用敲命令。只有 Python 需要装一次。

---

## 第一步：装 Python（只做一次，3分钟）

打开 **Microsoft Store**（Windows 自带的应用商店）→ 搜索 **Python** → 点 **获取**。

> 也可以去 python.org/downloads 下载，但安装时**务必勾选** `Add Python to PATH`。
> 用应用商店装就没这个坑，推荐新手用应用商店。

装完不用管，继续下一步。

---

## 第二步：下载本系统

打开这个链接，会直接下载一个 zip 压缩包：

https://github.com/biohewei-hue/KMK/archive/refs/heads/claude/sentiment-cross-analysis-report-4yx71y.zip

下载后**解压**（右键 → 全部解压缩），得到一个文件夹。

---

## 第三步：双击 `开始.bat`

进入解压出来的文件夹，找到 **`开始.bat`**，双击它。

> 如果 Windows 弹出蓝色的"已保护你的电脑"提示：点 **更多信息** → **仍要运行**。
> （这是因为文件是从网上下载的，不是病毒。）

会出现一个菜单：

```
  [1]  首次安装        (第一次使用选这个，装完不用再选)

  [2]  登录 Alpha派    (弹浏览器，登录后点开「每日必看」)
  [3]  登录 韭研公社    (弹浏览器，登录后点开「关注」栏目)

  [4]  抓取今日数据 + 生成报告骨架      <<< 每天点这个

  [5]  检查凭证状态
  [6]  只抓免登录的数据源 (抓不通时的保底方案)

  [0]  退出
```

**按顺序操作**：

### 输入 `1` 回车 —— 首次安装

自动装好所有依赖，第一次比较慢（几分钟），等它跑完就行。

### 输入 `2` 回车 —— 登录 Alpha派

会**弹出一个浏览器窗口**，你在浏览器里：

1. 登录 Alpha派
2. 点开 **蓝宝书 → PaiPai总结 → 每日必看**
3. 等内容显示出来，**往下滚动几屏**
4. **直接关闭浏览器窗口**

回到黑色窗口，看到 `✅ 已登记为主列表接口` 就成功了。

### 输入 `3` 回车 —— 登录韭研公社

一样的流程，浏览器里点开 **关注 → 每日公社内容精选 / 学习笔记 / 盘前纪要**，
滚动几屏后关闭窗口。

### 输入 `4` 回车 —— 抓数据

几分钟跑完。**以后每天只需要做这一步。**

---

## 第四步：生成报告

抓完数据后，在这个文件夹里打开 **Claude Code**，说一句：

> 生成今日报告

Claude 会读取抓到的数据，写出完整的 21 章节报告，存到 `data\今天日期\report.md`。

---

## 以后每天

双击 `开始.bat` → 输入 `4` → 等它跑完 → 打开 Claude Code 说「生成今日报告」。

登录状态会自动保存，不用重复登录。

---

## 出问题怎么办

| 黑窗口里显示 | 说明 | 怎么办 |
| --- | --- | --- |
| 没有找到 Python | Python 没装好 | 回第一步，用应用商店装 |
| 未捕获到凭证请求头 | 没登录 | 重新选 [2] 或 [3]，先登录再点栏目 |
| 未捕获到返回列表数据的接口 | 栏目内容没加载出来 | 重来一次，多滚动几屏再关窗口 |
| 鉴权失败 / authorization 已过期 | 凭证过期了 | 重新选 [2] 或 [3] 登录一次 |
| 某个源报错，其他正常 | 单个源的问题 | 不影响其他章节，可先选 [6] 出保底报告 |

**任何看不懂的报错**：把黑窗口里的内容截图或复制给 Claude，直接帮你定位。

---

## 关于凭证安全

- 登录信息存在你自己电脑的 `config\credentials.json`，**不会上传到 GitHub**
- 浏览器登录态存在 `config\.browser_profile\`，同样不上传
- 这两个位置都已在 `.gitignore` 中排除
