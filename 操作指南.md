# 操作指南（Windows）

> 命令在 **PowerShell** 或 **命令提示符** 里运行。若 `python` 提示找不到，改用 `py`。

## 一、首次准备（只做一次）

```powershell
# 1. 安装 Python 3.10+（python.org 下载时勾选 "Add Python to PATH"）
python --version

# 2. 把仓库拉到本地
git clone https://github.com/biohewei-hue/KMK.git
cd KMK
git checkout claude/sentiment-cross-analysis-report-4yx71y

# 3. 装依赖
pip install -r requirements.txt
playwright install chromium
```

然后新建 `config\credentials.json`（可先复制模板再改）：

```powershell
copy config\credentials.example.json config\credentials.json
notepad config\credentials.json
```

## 二、Alpha派：完整操作流程

### 第 1 步：抓凭证 + 找接口

```powershell
python tools\harvest_token.py alphapai
```

**会发生什么**：弹出一个 Chromium 浏览器窗口，自动打开 Alpha派。

**你要做什么**（在浏览器里）：

1. 如果没登录 → **先登录**
2. 点开 **蓝宝书 → PaiPai总结 → 每日必看**
3. 等列表内容显示出来，**往下滚动几屏**（滚动会触发翻页请求，能多抓到接口）
4. 完成后 **直接关闭浏览器窗口**

**回到终端看输出**，成功的话长这样：

```
✅ 凭证已更新 → config\credentials.json
   authorization: eyJhbGciOiJIUzI1NiJ9.eyJqdGk...
   x-device: 043ccffd2042321bc417c295...

发现 3 个返回列表的接口：
  [0]  20条  https://alphapai-web.rabyte.cn/external/alpha/api/reading/...
        样例: 每日必看：存储涨价链条梳理
        ✅ 已登记为主列表接口
```

看到 **"✅ 已登记为主列表接口"** 就说明接口找到了，这是最关键的一行。

### 第 2 步：验证凭证

```powershell
python tools\check_token.py
```

应显示 `✅ Alpha派 JWT 有效至 ...（剩余 N 天）`。

### 第 3 步：单独试抓 Alpha派

先只跑这一个源，确认没问题再跑全部：

```powershell
python run_daily.py --fetch --only alphapai
```

成功会显示 `[alphapai] ✅ 已保存 data\2026-08-03\alphapai.json`。

打开那个 json 看看有没有真实内容：

```powershell
notepad data\2026-08-03\alphapai.json
```

### 第 4 步：跑全量 + 生成报告骨架

```powershell
python run_daily.py --all --refresh
```

完成后在本仓库打开 Claude Code，说「**生成今日报告**」。

## 三、韭研公社

和 Alpha派 完全一样，只是把命令里的 `alphapai` 换成 `jiuyan`，
浏览器里要点开的栏目是 **关注 → 每日公社内容精选 / 学习笔记 / 公告内容精选 / 盘前纪要**：

```powershell
python tools\harvest_token.py jiuyan
python run_daily.py --fetch --only jiuyan
```

## 四、知识星球

星球用的是 cookie（有效期较长），不需要浏览器抓取，直接填进 `credentials.json` 即可。
星球 ID 不用找，程序会按「黑金」关键词自动定位。

## 五、以后每天怎么跑

```powershell
cd KMK
python run_daily.py --all --refresh
```

`--refresh` 会自动续期凭证（用第一次保存的登录态，无需再登录）。
然后打开 Claude Code 说「生成今日报告」。

## 六、出问题怎么办

| 现象 | 原因与处理 |
| --- | --- |
| `未捕获到凭证请求头` | 没登录，或没点开目标栏目。重跑并确认登录后再点栏目 |
| `未捕获到返回列表数据的接口` | 栏目内容没加载出来。重跑，多滚动几屏再关窗口 |
| `鉴权失败（authorization 可能已过期）` | 重跑 `python tools\harvest_token.py alphapai` |
| `playwright` 报找不到浏览器 | 跑一次 `playwright install chromium` |
| 抓取报错但想先出报告 | 免凭证的五个源照常可用：`python run_daily.py --all --only eastmoney,ths,cls,wscn,xueqiu` |

把终端的完整输出发给 Claude，可以直接帮你定位。
